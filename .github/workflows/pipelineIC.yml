name: workflow
on:
  push:
    branches:
      - segunda-rama

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: aqui compila
        run: |
          echo 'aqui compila'

  
  test:
    runs-on: ubuntu-latest
    steps:

      - name: testing
        run: |
          echo 'testing'

  
  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: deploy dsr
        run: |
          echo 'deploy dsr
