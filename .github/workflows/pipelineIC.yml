name: workflow
on:
  push:
    branches:
      - segunda-rama

jobs:
  sast:
    runs-on: ubuntu-latest
    steps:

    - name: set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'